apiVersion: v1
kind: Service
metadata:
  name: index-api-stable
  labels:
    app: index-api-stable
spec:
  selector:
    app: index-api-stable
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
